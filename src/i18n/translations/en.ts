// English locale settings (minimal since English phrases are used as keys)
// This file mainly contains locale-specific formatting rules

export const translations: Record<string, string> = {
  // Only include translations that need different formatting or special cases
  // Most text will use the English phrase keys directly
};

export default translations; 